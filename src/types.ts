export interface Color {
  bgColor: string;
  droopShadowColor: string;
  activeColor: string;
}

export interface Transaction {
  date: string;
  value: string;
  transactionFee: string;
  url: string;
}
